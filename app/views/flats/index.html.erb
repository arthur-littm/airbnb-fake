<h1>Our flats</h1>

<ul>
  <% @flats.each do |flat| %>
    <li><%= link_to flat["name"], flat_path(flat["id"]) %></li>
    <img src="<%= flat["imageUrl"] %>" alt="_blank" width="100px">
  <% end %>

</ul>
